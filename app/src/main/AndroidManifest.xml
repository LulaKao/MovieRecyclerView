<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.SharedPreferences">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/video"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/video"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.SharedPreferences.FinishPage"></activity>
        <activity android:name="com.example.SharedPreferences.InformationPage" />
        <activity android:name="com.example.SharedPreferences.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>